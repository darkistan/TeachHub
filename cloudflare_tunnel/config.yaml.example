# Приклад конфігурації Cloudflare Tunnel
# Цей файл створюється автоматично при встановленні
# Не редагуйте вручну - використовуйте install_tunnel.bat

tunnel: YOUR_TUNNEL_TOKEN_HERE
credentials-file: config/credentials.json

ingress:
  - hostname: teachhub.example.com
    service: http://localhost:5000
  - service: http_status:404

