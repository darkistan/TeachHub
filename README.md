# 🤖 Schedule Bot v2.0 - SQLite + Веб-інтерфейс

Telegram бот для управління навчальним розкладом з SQLite базою даних та Flask веб-інтерфейсом.

## 🚀 Швидкий старт

### Встановлення

```bash
# Клонування та встановлення залежностей
git clone <repository_url>
cd schedule_bot
python -m venv venv
venv\Scripts\activate  # Windows
# або source venv/bin/activate  # Linux/Mac
pip install -r requirements.txt
```

### Конфігурація

Створіть `config.env` на основі `config.env.example`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token
ADMIN_USER_ID=your_telegram_id
AIR_ALERT_CITY=Дніпро
ALERTS_API_TOKEN=your_alerts_token
DATABASE_URL=sqlite:///schedule_bot.db
FLASK_SECRET_KEY=your_secret_key_here
```

**АБО** просто запустіть `setup.bat` - він все зробить автоматично!

### Запуск

**Windows (.bat файли):**
```batch
setup.bat       # Налаштування (перший раз)
start_all.bat   # Запуск бота + веб
```

**Вручну:**
```bash
# Telegram бот
python bot.py

# Веб-інтерфейс (в іншому терміналі)
python run_web.py
# Відкрити: http://127.0.0.1:5000
```

---

## 📋 Функціонал

### Telegram бот
- 📅 **Розклад** - на день/тиждень з чисельником/знаменником
- ⏰ **Таймер** - візуальний таймер поточної пари
- 📊 **Прогрес навчання** - аналіз навчального року
- 🔔 **Нагадування** - за 10 хв до занять
- 📢 **Оголошення** - дошка оголошень
- 🚨 **Тривоги** - статус повітряних тривог
- 🔐 **Авторизація** - доступ через адміна

### Веб-інтерфейс
- 📊 **Dashboard** - статистика системи, останні логи
- 👥 **Користувачі** - управління доступом, схвалення запитів
- 📅 **Розклад** - перегляд та редагування
- 📢 **Оголошення** - створення та розсилка
- 📋 **Логи** - перегляд з фільтрами та пагінацією
- ⚙️ **Налаштування** - конфігурація системи
- 📊 **Статистика** - топ команд, активність по днях

---

## 🗂️ Структура проекту

```
schedule_bot/
├── models.py              # SQLAlchemy моделі (10 таблиць)
├── database.py            # Управління БД
├── bot.py                 # Telegram бот
├── run_web.py             # Запуск веб-інтерфейсу
│
├── auth_db.py             # Авторизація (БД версія)
├── schedule_handler.py    # Управління розкладом
├── notification_manager.py # Нагадування
├── announcement_manager.py # Оголошення
├── air_alert.py           # Моніторинг тривог
├── logger.py              # Логування (файл + БД)
│
├── web_admin/             # Flask веб-інтерфейс
│   ├── app.py            # Flask додаток
│   ├── templates/        # HTML шаблони
│   │   ├── base.html     # Базовий шаблон (Bootstrap 5)
│   │   ├── dashboard.html
│   │   ├── users.html
│   │   ├── schedule.html
│   │   ├── announcements.html
│   │   ├── logs.html
│   │   ├── settings.html
│   │   └── stats.html
│   └── static/
│       ├── css/style.css
│       └── js/main.js
│
├── backup/                # Backup JSON файлів
├── config.env             # Конфігурація (не в git!)
├── config.env.example     # Template конфігурації
├── .gitignore            # Git ignore
├── requirements.txt       # Залежності
└── schedule_bot.db        # База даних SQLite
```

### База даних

10 таблиць SQLite:
- `users` - користувачі з доступом
- `pending_requests` - запити на доступ
- `schedule_entries` - заняття розкладу
- `schedule_metadata` - метадані (група, рік, тиждень)
- `academic_periods` - періоди навчання
- `announcements` - оголошення
- `notification_history` - історія оповіщень
- `notification_settings` - налаштування оповіщень
- `logs` - системні логи
- `bot_config` - конфігурація (key-value)

---

## 📱 Команди бота

### Користувачі
- `/start` - перевірка доступу
- `/menu` - головне меню
- `/today` - розклад на сьогодні
- `/week` - розклад на тиждень
- `/help` - довідка

### Адміністратор
- `/admin` - панель управління

---

## 🌐 Веб-інтерфейс

### 📊 Dashboard `/`
- Статистика: користувачі, розклад, оголошення, запити
- Останні 10 логів системи
- Поточний стан: група, рік, тип тижня

### 👥 Користувачі `/users`
- ➕ Додавання користувачів (User ID, username)
- 🗑️ Видалення користувачів
- ✅ Схвалення запитів на доступ
- ❌ Відхилення запитів
- 📋 Таблиця з сортуванням

### 📅 Розклад `/schedule`
- Перегляд по днях тижня
- Чисельник/знаменник
- (Редагування буде додано в наступних версіях)

### 📢 Оголошення `/announcements`
- Список всіх оголошень
- Створення/редагування/видалення
- Розсилка оповіщень

### 📋 Логи `/logs`
- Фільтр по рівню (INFO/WARNING/ERROR/SECURITY)
- Пошук по тексту
- Пагінація (100 записів на сторінку)
- Кольорове виділення

### ⚙️ Налаштування `/settings`
- Назва групи
- Навчальний рік
- Поточний тип тижня
- Зберігає в БД

### 📊 Статистика `/stats`
- Топ-10 команд
- Активність по днях
- Кількість користувачів

---

## 🔧 Розробка

### Додавання нової моделі БД

```python
# models.py
class NewModel(Base):
    __tablename__ = 'new_table'
    id = Column(Integer, primary_key=True)
    name = Column(String(100))

# Використання
from database import get_session
from models import NewModel

with get_session() as session:
    item = NewModel(name="Test")
    session.add(item)
```

### Backup БД

```bash
# Windows
copy schedule_bot.db backup\schedule_bot_backup.db

# Linux/Mac
cp schedule_bot.db backup/schedule_bot_backup.db
```

---

## 🛠️ Troubleshooting

### Бот не запускається
- Перевірте `TELEGRAM_BOT_TOKEN` в `config.env`
- Перевірте `ADMIN_USER_ID`
- Переконайтесь що є файл `schedule_bot.db`

### Веб не запускається
- Встановіть залежності: `pip install -r requirements.txt`
- Перевірте чи не зайнятий порт 5000
- Запустіть: `python run_web.py`

### Пусті дані
- Запустіть міграцію: `python migrate_to_db.py`
- Або додайте дані вручну через веб

---

## 🔐 Безпека

- ✅ CSRF захист для всіх форм
- ✅ Авторизація через адміна
- ✅ Валідація вводу
- ✅ Детальне логування дій
- ✅ Веб тільки на localhost
- ✅ Секрети в .env файлі (не в git!)

---

## 🔒 Конкурентний доступ (веб + бот)

### Як працює:

**Проблема:** SQLite блокується при одночасному запису.

**Рішення (реалізовано):**
- ✅ **WAL Mode** - одночасне читання під час запису
- ✅ **Busy Timeout 30s** - очікування розблокування
- ✅ **Retry Logic** - 3 автоматичні спроби
- ✅ **Connection Pool** - до 30 з'єднань
- ✅ **Оптимізовані PRAGMA** - швидкість + безпека

### Перевірка статусу:

```bash
# Діагностика БД
python check_db_status.py

# Увімкнення WAL (якщо потрібно)
python check_db_status.py wal

# Очищення WAL журналу
python check_db_status.py checkpoint
```

**Детально:** Дивіться [`DB_CONCURRENCY_GUIDE.md`](DB_CONCURRENCY_GUIDE.md)

---

## 📊 Changelog

### v2.0.0 (2025-12-04)
- ✨ Міграція на SQLite БД
- 🌐 Flask веб-інтерфейс
- 📊 Dashboard зі статистикою
- 💾 Система backup
- 📝 Оновлена документація
- 🔒 CSRF захист
- 📋 Логування в БД

### v1.0.0
- 🤖 Початкова версія з JSON зберіганням

---

## 📄 Ліцензія

MIT License

---

**Проект готовий до використання!** 🎉

Для питань: перевірте логи в `logs.txt` або веб-інтерфейс `/logs`
