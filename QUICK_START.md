# ⚡ Швидкий старт Schedule Bot v2.0

## 📝 Checklist перед запуском

- [ ] Python 3.8+ встановлено
- [ ] Git clone завершено
- [ ] Віртуальне середовище створено
- [ ] Залежності встановлені
- [ ] config.env створено з токенами
- [ ] Міграція виконана (якщо є JSON)

---

## 🚀 5 кроків до запуску

### Крок 1: Клонування та середовище
```bash
git clone <repository_url>
cd schedule_bot
python -m venv venv
venv\Scripts\activate  # Windows
```

### Крок 2: Встановлення залежностей
```bash
pip install -r requirements.txt
```

### Крок 3: Конфігурація
```bash
# Скопіюйте template
copy config.env.example config.env

# Відредагуйте config.env:
# - TELEGRAM_BOT_TOKEN=... (отримайте у @BotFather)
# - ADMIN_USER_ID=... (ваш Telegram ID)
# - ALERTS_API_TOKEN=... (з alerts.in.ua)
```

### Крок 4: Запуск

**Термінал 1 - Telegram бот:**
```bash
python bot.py
```

**Термінал 2 - Веб-інтерфейс:**
```bash
python run_web.py
# Відкрити: http://127.0.0.1:5000
```

---

## ✅ Перевірка що все працює

### 1. Перевірте БД:
```bash
python check_db_status.py
# Має показати WAL mode та таблиці
```

### 2. Перевірте бота:
- Відкрийте Telegram
- Знайдіть вашого бота
- Напишіть `/start`
- Маєте побачити меню

### 3. Перевірте веб:
- Відкрийте http://127.0.0.1:5000
- Маєте побачити Dashboard
- Перевірте розділи "Користувачі", "Логи"

---

## 🆘 Проблеми?

### Бот не відповідає
```bash
# Перевірте токен
grep TELEGRAM_BOT_TOKEN config.env

# Перевірте чи бот запущений
# Має бути "Бот запущено!" в консолі
```

### Веб не відкривається
```bash
# Перевірте чи порт 5000 вільний
netstat -ano | findstr :5000  # Windows

# Перевірте чи веб запущений
# Має бути "Running on http://127.0.0.1:5000" в консолі
```

### "Database is locked"
```bash
# Увімкніть WAL mode
python check_db_status.py wal

# Перезапустіть бота та веб
```

### Пусті дані
```bash
# Додайте дані через веб-інтерфейс
# Перейдіть в "Користувачі" → "Додати користувача"
# Або "Розклад" → "Додати заняття"
```

---

## 📚 Наступні кроки

1. **Додайте себе як користувача:**
   - Дізнайтеся свій Telegram ID (@userinfobot)
   - Веб → Користувачі → Додати

2. **Додайте розклад:**
   - Веб → Розклад → Додати заняття
   - Або імпортуйте з JSON через міграцію

3. **Налаштуйте оповіщення:**
   - В боті: Меню → Увімкнути оповіщення
   - Веб → Налаштування → Час оповіщення

4. **Створіть оголошення:**
   - Веб → Оголошення → Створити
   - Виберіть пріоритет
   - Розішліть користувачам

---

## 🎯 Готово!

**Бот працює:** Telegram команди /start, /menu, /today, /week  
**Веб працює:** http://127.0.0.1:5000

**Успіхів!** 🎉

---

## 📖 Детальна документація

- [`README.md`](README.md) - повна документація проекту
- [`USER_GUIDE.md`](USER_GUIDE.md) - інструкція веб-інтерфейсу
- [`DB_CONCURRENCY_GUIDE.md`](DB_CONCURRENCY_GUIDE.md) - конкурентний доступ

