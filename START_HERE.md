# 🚀 ПОЧАТОК РОБОТИ - Schedule Bot v2.0

## ⚡ Швидкий старт (3 команди)

```batch
REM 1. Налаштування (один раз)
setup.bat

REM 2. Запуск всієї системи (бот + веб)
start_all.bat

REM 3. Відкрити в браузері
start http://127.0.0.1:5000
```

---

## 📁 Файли запуску (.bat)

### 🔧 `setup.bat` - Налаштування (запустити ОДИН РАЗ)
**Що робить:**
- ✅ Перевіряє Python
- ✅ Створює віртуальне середовище (venv)
- ✅ Встановлює залежності (pip install -r requirements.txt)
- ✅ Створює config.env з шаблону
- ✅ Запускає міграцію даних (якщо є JSON)
- ✅ Перевіряє WAL mode БД

**Запуск:**
```batch
setup.bat
```

---

### 🤖 `start_bot.bat` - Telegram бот
**Що робить:**
- ✅ Активує venv
- ✅ Перевіряє config.env
- ✅ Перевіряє БД
- ✅ Запускає бота (python bot.py)

**Запуск:**
```batch
start_bot.bat
```

**Команди бота:** `/start`, `/menu`, `/today`, `/week`, `/admin`, `/help`

---

### 🌐 `start_web.bat` - Веб-інтерфейс
**Що робить:**
- ✅ Активує venv
- ✅ Перевіряє config.env та БД
- ✅ Запускає Flask (python run_web.py)
- ✅ Відкриває http://127.0.0.1:5000

**Запуск:**
```batch
start_web.bat
```

**Сторінки:** Dashboard, Користувачі, Розклад, Оголошення, Логи, Статистика, Налаштування

---

### 🚀 `start_all.bat` - Запуск ВСЬОГО (рекомендовано)
**Що робить:**
- ✅ Запускає бота в окремому вікні
- ✅ Запускає веб в окремому вікні
- ✅ Обидва працюють паралельно

**Запуск:**
```batch
start_all.bat
```

**Результат:** 2 вікна - НЕ закривайте їх!
- Вікно 1: Telegram бот (лог подій)
- Вікно 2: Веб-інтерфейс (Flask сервер)

---

## ✅ Checklist першого запуску

- [ ] 1. Python 3.8+ встановлено
- [ ] 2. Git clone виконано
- [ ] 3. Запущено `setup.bat`
- [ ] 4. Відредаговано `config.env` (токени)
- [ ] 5. База даних створена
- [ ] 6. Запущено `start_all.bat`
- [ ] 7. Бот відповідає в Telegram
- [ ] 8. Веб відкривається http://127.0.0.1:5000

---

## 🆘 Якщо щось не працює

### ❌ setup.bat видає помилку
```batch
REM Перевірте Python
python --version
REM Має бути >= 3.8

REM Встановіть вручну
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

### ❌ Бот не запускається
```batch
REM Перевірте токен в config.env
notepad config.env

REM Перевірте БД
python check_db_status.py
```

### ❌ Веб не відкривається
```batch
REM Перевірте чи порт 5000 вільний
netstat -ano | findstr :5000

REM Змініть порт в run_web.py (останній рядок)
REM app.run(host='127.0.0.1', port=5001, debug=True)
```

### ❌ "Database is locked"
```batch
REM Увімкніть WAL mode
python check_db_status.py wal

REM Перезапустіть бота та веб
```

---

## 📝 Важливі файли

| Файл | Призначення | Можна видаляти? |
|------|-------------|-----------------|
| `config.env` | Токени та налаштування | ❌ НІ |
| `schedule_bot.db` | База даних | ❌ НІ |
| `schedule_bot.db-wal` | WAL журнал | ❌ НІ |
| `logs.txt` | Текстові логи | ✅ Можна очистити |
| `backup/` | Backup JSON і БД | ✅ Можна очистити старі |

---

## 🎯 Що далі?

### Після запуску:

1. **Додайте себе як користувача:**
   - Веб → Користувачі → Додати (ваш Telegram ID)

2. **Додайте розклад:**
   - Веб → Розклад → (буде форма додавання)
   - Або імпортуйте через міграцію

3. **Створіть оголошення:**
   - Веб → Оголошення → Створити

4. **Увімкніть оповіщення:**
   - Telegram → /menu → Увімкнути оповіщення

---

## 📚 Документація

- [`README.md`](README.md) - повна документація проекту
- [`USER_GUIDE.md`](USER_GUIDE.md) - інструкція веб-інтерфейсу
- [`QUICK_START.md`](QUICK_START.md) - швидкий старт за 5 кроків
- [`DB_CONCURRENCY_GUIDE.md`](DB_CONCURRENCY_GUIDE.md) - конкурентний доступ
- [`MIGRATION_SUMMARY.md`](MIGRATION_SUMMARY.md) - звіт про міграцію

---

## ✨ Готово!

**Запустіть:**
```batch
start_all.bat
```

**І почніть користуватися!** 🎉

- 🤖 Бот: Telegram → Ваш бот → /start
- 🌐 Веб: http://127.0.0.1:5000

---

**Успіхів з Schedule Bot v2.0!** 🚀

